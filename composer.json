{
    "name": "dgrochowina/projekt-rezerwacji",
    "description": "Aplikacja do rezerwacji stolików w restauracji",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "vlucas/phpdotenv": "^5.5"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "process-timeout": 90000
    },
    "scripts": {
        "serve": "php -S localhost:8000 -t public"
    }
}